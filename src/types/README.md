# src/types/

Tipos compartidos transversales al sistema (no específicos de un módulo).

Ejemplos válidos: tipos de utilidad, tipos generados de Supabase (`database.types.ts`), branded types primitivos.

**NO permitido**: tipos de dominio de un módulo (viven en `src/features/<module>/domain/types.ts`).
